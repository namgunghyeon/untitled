machine:
  node:
    version: 8.1.2

dependencies:
  pre:
    - npm install node-sass

test:
  override:
    - echo "No Tests"

deployment:
  master:
    branch: master
    commands:
      - ssh root@45.32.38.24 'cd /root/test && git clone git@github.com:namgunghyeon/untitled_api.git'
