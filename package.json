{
  "name": "study",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "nodemon": "^1.11.0",
    "pre-commit": "^1.2.2"
  },
  "scripts": {
    "eslint-client": "cd client && node_modules/.bin/eslint ."
  },
  "pre-commit": {
    "run": [
      "eslint-client"
    ]
  }
}
